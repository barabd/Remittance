export const REMITTANCE_QUEUE_EVENT = 'remittanceQueue:changed'

export const REMITTANCE_QUEUE_LS_KEY = 'frms.remittanceQueue.active.v1'

/** Passed to live API as checker identity (must differ from row.maker for approval). */
export const DEFAULT_QUEUE_CHECKER_USER = 'HO-Checker-01'

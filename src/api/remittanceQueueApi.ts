/**
 * Approvals queue — GET `/remittances/queue`, POST `/:id/approve`, POST `/:id/reject`.
 *
 * Java: `com.frms.ops.remittance` + table `remittance_queue_item`. Merge: `src/integrations/remittanceQueue/`.
 * Enable with `VITE_USE_LIVE_API=true`.
 */

export {}
